[project]
name = "microurl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Mapande", email = "mapandesamuel27@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi[standard]>=0.141.1",
    "slowapi>=0.1.10",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
microurl = "microurl:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
